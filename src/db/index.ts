// DB module stub — mock data used in API routes for MVP.
// bun:sqlite doesn't work with Next.js webpack builds, so we keep
// the DB client in separate scripts (e.g., seed.ts) that run directly with bun.
export const db = null;
